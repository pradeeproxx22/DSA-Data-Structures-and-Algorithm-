# Find the larget and second largest element but not distinct element 

n = int(input("Element enter :"))
arr = []

for i in range(0,n):
    value = int(input(": "))
    arr.append(value)


largest = arr[0] 
scnd_largest = None
        

for i in range(1, n):
    if arr[i] > largest:
         scnd_largest = largest
         largest = arr[i]       #Largest update

    elif  arr[i] == largest : 
         print(arr)
         pass

    elif scnd_largest is None:
         #first distinct candidate
         scnd_largest = arr[i]


    elif arr[i] > scnd_largest : 
         scnd_largest = arr[i]
    


print("Larget :",largest ,"second_Largest: ", scnd_largest)


