# Find the larget and second largest element but not distinct element 

n = int(input("Element enter :"))
arr = []

for i in range(0,n):
    value = int(input(": "))
    arr.append(value)


largest = arr[0] 
scnd_largest = None
        
if scnd_largest is None :  # This line means : i don't find any valid value of second_largest element 
   largest == arr[i] 
      


for i in range(2, n):
    if arr[i] > largest:
         scnd_largest = largest
         largest = arr[i]
    elif  scnd_largest < arr[i] and arr[i] != largest : 
         scnd_largest = arr[i]
    


print("Larget :",largest ,"second_Largest: ", scnd_largest)


